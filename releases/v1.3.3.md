# Agent Beacon v1.3.3

## 更新内容

- 删除 Codex 和 TRAE 的通用界面兜底。
- 完全移除 TRAE UIAutomation、截图识别、日志和 Work 会话发现与解析。
- Codex 仅使用本地 session JSONL；TRAE 仅接受 `integration=mcp` 的主动生命周期事件。
- TRAE MCP 工具定义缩减为 `state + session_id`，全局规则压缩为一段，工具响应改为 `ok`，减少固定上下文和工具调用 token。
