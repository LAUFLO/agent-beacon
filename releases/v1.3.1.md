# Agent Beacon v1.3.1

## 更新内容

- 将 TRAE MCP Helper 迁移到固定路径 `Agent-Beacon-MCP.exe`。
- 主程序启动时按 SHA-256 比较内嵌 Helper；内容变化时原子替换，相同内容不重复写入。
- Helper 被 TRAE 占用时暂存为 `.pending.exe`，并在后续启动重试。
- 不再保留旧 `Agent-Beacon-MCP-1.3.0.exe`。
- 从 v1.3.0 升级后需在 TRAE 中删除旧 MCP 并重新粘贴一次配置；后续升级不再改变 MCP 命令路径。
