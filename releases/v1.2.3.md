# Agent Beacon v1.2.3

## 更新内容

- 适配 TRAE Work 将确认卡和最终回复包装为 `progressMessage` 的新结构。
- 等待用户回复的语义优先标黄，明确结束且没有后续动作的语义进入候选完成。
- 视觉确认检测不再要求 TRAE 必须是前台窗口；后台可见窗口仍可建立黄色锁。
- 只有 TRAE 在前台且确认标记确实消失时才能清除黄色锁，避免切换窗口或截图时黄灯丢失。
- 新版会通知 v1.2.2 及更早版本的托盘进程退出。
