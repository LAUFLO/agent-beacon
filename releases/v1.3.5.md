# Agent Beacon v1.3.5

## 更新内容

- 修复 TRAE 在用户回复后直接继续执行、但漏调 `state=running` 导致黄灯保持的问题。
- 全局规则将“waiting 后收到回复，第一步必须上报 running”提升为最高优先级。
- MCP 的 `waiting` 返回增加简短恢复提醒，其他状态仍只返回 `ok`。
- 只在发生等待时增加极少量 token；TRAE 仍保持纯 MCP，不恢复本地日志、UI 或截图兜底。
