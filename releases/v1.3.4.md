# Agent Beacon v1.3.4

## 更新内容

- 修复 Codex 实际弹出命令确认，但 session JSONL 没有审批事件且调用参数没有 `require_escalated` 时漏黄灯的问题。
- Codex JSONL 现在记录尚未结束的 `exec`；只有存在这种调用时，才短暂检查 ChatGPT/Codex 当前可见窗口中的“允许”按钮和审批标题。
- 没有待结束命令时完全不运行 UIAutomation。
- TRAE 保持纯 MCP，TRAE 日志、Work 会话、UI 和截图兜底均不恢复。
