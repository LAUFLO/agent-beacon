# Agent Beacon v1.3.0

## 更新内容

- 为 TRAE 增加本地 MCP 主动上报通道。
- Agent Beacon 主程序内嵌轻量控制台 MCP Helper，TRAE 可通过标准输入输出上报 `running`、`waiting`、`completed`、`failed` 和 `cancelled`。
- MCP 事件优先于可变日志和后台心跳；等待状态立即黄灯闪烁，完成、失败或取消立即红灯。
- 只有新的任务 ID 或明确恢复事件才能变回绿色。
- 本版本未安装 MCP 时仍保留原有日志和界面识别作为降级方案。
